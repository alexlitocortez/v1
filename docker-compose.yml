version: '3.8'

services:
  db:
    image: mysql:latest
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Thisishowwedoitfeelme25!
      MYSQL_DATABASE: v1
      MYSQL_USER: Alex
      MYSQL_PASSWORD: Thisishowwedoitfeelme25!
    ports:
      - "3306:3306"
    volumes:
      - ./data:/var/lib/mysql

